{
  "name": "fitinn-app",
  "version": "1.0.0",
  "private": true,
  "description": "Fit-Inn Trier – native App-Hülle (Capacitor) um das Mitgliederportal (Live-URL-Wrapper + Push + Apple/Google-Login + Biometrie)",
  "scripts": {
    "setup": "npm install && npx cap add ios && npx cap add android && npx cap sync",
    "sync": "cap sync",
    "icons": "capacitor-assets generate",
    "ios": "cap sync ios && cap open ios",
    "android": "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/push-notifications": "^8.1.1",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.1"
  }
}
